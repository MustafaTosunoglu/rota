/**
 * Identity &amp; Access Management: users, roles, authentication, sessions.
 * Implementation arrives in Phase 1D.
 */
@ApplicationModule(displayName = "IAM")
package com.rota.iam;

import org.springframework.modulith.ApplicationModule;
