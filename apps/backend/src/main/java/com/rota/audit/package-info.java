/**
 * Append-only, hash-chained domain audit log. Implementation arrives in Phase 1E.
 */
@ApplicationModule(displayName = "Audit")
package com.rota.audit;

import org.springframework.modulith.ApplicationModule;
