/**
 * Public API of the documents module — exposed to other modules as the {@code api} named
 * interface (events, the version guard and the exceptions it throws).
 */
@org.springframework.modulith.NamedInterface("api")
package com.rota.documents.api;
