{
  "name": "rota",
  "version": "0.0.0",
  "private": true,
  "description": "Rota — interactive API documentation SaaS platform",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev:web": "pnpm --filter @rota/web dev",
    "build:web": "pnpm --filter @rota/web build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
